# NetVSA — setup/

This directory will hold OS setup helpers for the **real communication**
demo (Windows network profile check, firewall rule provisioning), added in
**Phase 5 — Real Communication (Agent)**, per Technical Architecture §4.2a
and Implementation Plan Phase 5 (`setup/setup-windows.ps1`).

Empty by design in Phase 1 — there is no real networking to configure yet.
