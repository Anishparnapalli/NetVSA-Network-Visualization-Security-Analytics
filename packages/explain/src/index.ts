/**
 * @netvsa/explain
 *
 * Phase 1 placeholder.
 *
 * This package will hold deterministic explanation generators — pure
 * `explain(event)` functions that turn an event's actual payload data into
 * the human-readable `explanation` field, for example on
 * FIREWALL_BLOCKED / FIREWALL_ALLOWED / THREAT_DETECTED
 * (Technical Architecture §9.3, Implementation Plan Phase 6).
 *
 * Intentionally empty in Phase 1.
 */

export const EXPLAIN_PACKAGE_PLACEHOLDER = "netvsa-explain-phase-1-placeholder" as const;
