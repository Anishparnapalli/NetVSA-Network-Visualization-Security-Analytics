/**
 * @netvsa/events
 *
 * Phase 1 placeholder.
 *
 * This package will hold the shared `NetVsaEvent` envelope and every event
 * type schema used across the Agent, the Simulation/Security engines, and
 * the web UI (Technical Architecture §6, Implementation Plan Phase 2).
 *
 * Intentionally empty in Phase 1 — populating it is Phase 2 scope
 * ("Event Pipeline & Static Topology Rendering"). Kept as a real, typed,
 * buildable package now so later phases only need to add files, not set
 * up tooling.
 */

export const EVENTS_PACKAGE_PLACEHOLDER = "netvsa-events-phase-1-placeholder" as const;
