/**
 * @netvsa/security
 *
 * Phase 1 placeholder.
 *
 * This package will hold the firewall rule-evaluation engine and the
 * deterministic IDS detection logic (rolling-window threshold detectors),
 * as pure, unit-testable functions (Technical Architecture §9,
 * Implementation Plan Phase 6).
 *
 * Intentionally empty in Phase 1.
 */

export const SECURITY_PACKAGE_PLACEHOLDER = "netvsa-security-phase-1-placeholder" as const;
