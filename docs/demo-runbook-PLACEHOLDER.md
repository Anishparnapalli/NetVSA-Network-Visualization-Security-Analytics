# NetVSA — Demo Runbook (placeholder)

The real `docs/demo-runbook.md` — network requirements, pre-flight steps,
and the corrected Wireshark verification method — is written in
**Phase 5 — Real Communication (Agent)**, per Implementation Plan Phase 5
and Technical Architecture §4.9.

This placeholder exists only so the `docs/` folder structure matches the
architecture document from Phase 1 onward.
