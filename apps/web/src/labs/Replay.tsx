import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Replay — Phase 1 placeholder.
 * Later: full event-log-based replay reusing the live rendering pipeline
 * (Implementation Plan Phase 8; Technical Architecture §7.6).
 */
export function Replay() {
  return (
    <LabPlaceholder
      title="Replay"
      description="Event-log-based session replay, reusing the live rendering pipeline, arrives in Phase 8."
    />
  );
}
