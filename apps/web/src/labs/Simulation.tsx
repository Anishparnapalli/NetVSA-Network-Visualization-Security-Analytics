import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Simulation — Phase 1 placeholder.
 * Later: sandbox topology builder + live simulation engine
 * (Implementation Plan Phase 3; Technical Architecture §5, §8.4).
 */
export function Simulation() {
  return (
    <LabPlaceholder
      title="Simulation"
      description="The stateful simulation engine (switching, routing, firewall, IDS) arrives starting Phase 3."
    />
  );
}
