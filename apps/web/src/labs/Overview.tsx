import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Overview — Phase 1 placeholder.
 * Later: mode-select landing content per Technical Architecture §8.1.
 */
export function Overview() {
  return (
    <LabPlaceholder
      title="Overview"
      description="NetVSA foundation is running. This screen becomes the mode-select landing (Real Network / Simulation / Guided Journey) in a later phase."
    />
  );
}
