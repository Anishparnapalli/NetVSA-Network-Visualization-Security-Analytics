import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Security Lab — Phase 1 placeholder.
 * Later: firewall rule editor, controlled IDS scenarios, security timeline
 * (Implementation Plan Phase 6; Technical Architecture §9).
 */
export function Security() {
  return (
    <LabPlaceholder
      title="Security Lab"
      description="Firewall rule editing and deterministic IDS detection arrive in Phase 6."
    />
  );
}
