import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Analytics — Phase 1 placeholder.
 * Later: session statistics dashboard computed from the recorded event
 * log (Implementation Plan Phase 8; Technical Architecture §39).
 */
export function Analytics() {
  return (
    <LabPlaceholder
      title="Analytics"
      description="Session statistics and dashboards, computed from the recorded event log, arrive in Phase 8."
    />
  );
}
