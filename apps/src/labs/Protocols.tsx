import { LabPlaceholder } from "./LabPlaceholder";

/**
 * Protocol Explorer — Phase 1 placeholder.
 * Later: TCP/UDP/ARP/HTTP/HTTPS-TLS exploration with real-vs-conceptual
 * split (Implementation Plan Phase 7; Technical Architecture §7.7).
 */
export function Protocols() {
  return (
    <LabPlaceholder
      title="Protocol Explorer"
      description="TCP, UDP, ARP, HTTP, and the real-vs-conceptual TLS split arrive in Phase 7."
    />
  );
}
